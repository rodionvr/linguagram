## uninstalled for now**

#from google import genai
#import os


# put key in env. var
### not currently working: api_key is returning as None, most likely an issue on my side



# putting on pause because not knowledgable enough

#client = genai.Client(api_key = os.getenv("GEMINI_API_KEY"))

#response = client.models.generate_content(
   # model = "gemini-2.5-flash",
   # contents = "short summary of ai"
#)


#print(response.text)